export const getGlobalScreenOptions = isDarkMode => ({
  headerStyle: {
    backgroundColor: isDarkMode ? '#424242' : '#2F80ED',
  },
  headerTintColor: '#fff',
  headerTitleStyle: {
    fontWeight: '500',
  },
});
